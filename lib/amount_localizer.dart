/// Locale-aware amount formatter with a native bridge that reads the
/// device's true Region setting (iOS) / format-locale country (Android).
library;

export 'src/amount_input_locale.dart';
export 'src/amount_localizer.dart';
